# Features

This directory is populated by the `/new-task` skill.

Each feature gets its own subdirectory:
```
features/
└── {feature-name}/
    ├── spec.md     # what to build + acceptance criteria
    └── tasks.md    # executable checklist
```

Run `/new-task` to scaffold a new feature.
